kind = "web"
previewPath = "/"
title = "Birthday Surprise"
version = "1.0.0"
id = "artifacts/birthday-surprise"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25201

[services.development]
run = "npm run client"

[services.production]
build = [ "npm", "run", "build" ]
publicDir = "artifacts/birthday-surprise/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25201"
BASE_PATH = "/"
